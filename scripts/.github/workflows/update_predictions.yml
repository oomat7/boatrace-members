name: Update predictions

on:
  # 毎日 7:00 JST に実行（= 22:00 UTC）
  schedule:
    - cron: '0 22 * * *'
  # 手動実行もできるようにしておくとデバッグに便利
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      # リポジトリのコードをチェックアウト
      - name: Checkout
        uses: actions/checkout@v4

      # Node.js をセットアップ
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 必要なパッケージをインストール
      - name: Install dependencies
        run: npm install @supabase/supabase-js dotenv openai

      # 🔧 いったんレース情報の更新は止める（手入力の選手名を消さないため）
      #    JSON から自動で予想を入れ直したいタイミングでコメントアウトを解除します
      # - name: Run update script
      #   run: node scripts/update_predictions.js
      #   env:
      #     SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      #     SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      # AI コメント生成
      - name: Run AI prediction generator
        run: node scripts/generate_ai_prediction.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
